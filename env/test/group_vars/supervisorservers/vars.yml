---

# NOTE: supervisord_inet_pass is duplicated in the host config for galaxy07 in the main env for Main's k8s runner

supervisord_install: package

supervisord_inet_user: "{{ vault_supervisord_inet_user }}"
supervisord_inet_listen: "{{ vault_supervisord_inet_listen }}"
supervisord_inet_port: "{{ vault_supervisord_inet_port }}"
supervisord_inet_pass: "{{ vault_supervisord_inet_pass }}"
